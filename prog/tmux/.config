;; -*- mode: clojure -*-
(
 (:packages (:apt "tmux")
            (:msys "tmux")
            (:yay "tmux-git"))

 (:packages (:gem "tmuxinator"))

 (:shell
  {:cmd ("tic -x ./terminfo/tmux.terminfo"
         "tic -x ./terminfo/tmux-24bit.terminfo")
   :desc "Installing tmux terminfo"})

 (:link "theme" "$XDG_CONFIG_HOME/tmux/theme"
        "tmuxrc" "$XDG_CONFIG_HOME/tmux/tmux.conf"
        {:src "layouts/*" :dest "$XDG_CONFIG_HOME/tmuxinator/" :glob true})

 (:mkdirs "$XDG_CACHE_HOME/tmux")

 (:import "cmds")
)

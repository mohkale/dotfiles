# -*- mode: conf-space -*- GNU GDB Mammon Config Initialisation File -- vim:syntax=conf:filetype=conf

# Seems really useful... but triggers the PAGER after every action which is really annoying.
set $SHOW_CONTEXT = 0

;; -*- mode: clojure -*-
(
 (:packages (:choco "cmdermini"))

 ;; I know, long and weird config name, but that's what it is
 (:link {:src "config.xml" :dest "c:/tools/cmdermini/vendor/conemu-maximus5/ConEmu.xml"
         :symbolic false :force true})
)

# -*- mode: yaml -*-
- packages:
  - pacman: wget

- shell:
    - description: Generating WgetRC File
      quiet: true
      command: |-
        dest=$HOME/.config/wgetrc
        cp -f wgetrc "$dest"
        sed -i -e 's ~ '"$HOME"' g' "$dest"

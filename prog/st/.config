;; -*- mode: clojure -*-
(
 (:shell {:desc "Installing st, the Suckless Terminal"
          :cmd "git -C src submodule sync --quiet --recursive
                if ! [ -e src/.git ]; then
                  git submodule update --init --recursive src
                fi

                cd src
                make install"})
 )

# [[https://github.com/adrienverge/yamllint][yamllint]] configuration file -*- mode: yaml -*- vim: syntax=yaml filetype=yaml

extends: default

rules:
  # 80 chars should be enough, but don't fail if a line is longer
  line-length:
    # I often insert org-links which can get pretty massive.
    # Might be worth disabling this altogether.
    max: 250
    level: warning

  # don't bother me with this rule
  indentation: disable
  truthy: disable
  comments-indentation: disable

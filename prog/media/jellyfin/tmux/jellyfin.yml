---
session_name: jellyfin
start_directory: ~/

windows:
  - window_name: daemon
    panes:
      - shell_command:
          - eval "$(jellyfin-env)" || exit 1
          - trap 'jellyfin-docker down' EXIT
          - clear && jellyfin-docker up

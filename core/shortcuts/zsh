# -*- mode: sh -*-
commands_dir="$XDG_CONFIG_HOME/shortcuts/commands"

fpath+="$commands_dir"

while read file; do
    autoload "$file"
done < <(find "$commands_dir" -type f -or -type l)

unset commands_dir

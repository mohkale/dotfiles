# -*- mode: sh -*-
# bash doesn't have the concept of autoloads, so
# you're going to have to instead load everything.

while read file; do
    source "$file"
done < <(find -L "$XDG_CONFIG_HOME/shortcuts/commands" -maxdepth 1 -type f)

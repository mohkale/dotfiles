;; -*- mode: clojure -*-
(
 ;; TODO: Fix error when clean path doesn't exist.
 (:when "test -e ~/.local/bin"
   (:clean "~/.local/share/fonts"))

 (:link {:src "*.ttf" :dest "~/.local/share/fonts/" :glob true})
 (:link {:src "*.otf" :dest "~/.local/share/fonts/" :glob true})
)

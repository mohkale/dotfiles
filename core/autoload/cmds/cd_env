# -*- mode: sh -*-
cd_env() {
    select_path "$(env-paths -d |
      if hash column 2>/dev/null; then column -s = -t; else tr '=' ' '; fi |
      FZF_DEFAULT_OPTS="$FZF_SHELL_OPTS" fzf --no-multi -d ' ' --preview 'preview {2..}' --preview-window=25% |
      cut -d ' ' -f 2- | sed 's/^ *//')"
}


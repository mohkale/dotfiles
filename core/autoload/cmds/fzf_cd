# -*- mode: sh -*-
fzf_cd() {
    local dest=$(ls-project -dz "$@" |
      FZF_DEFAULT_OPTS="$FZF_SHELL_OPTS" fzf --read0 --no-multi --preview 'preview {}' --preview-window=25%)
    cd "$dest" || echo "error: failed to cd: $dest" >&2
}

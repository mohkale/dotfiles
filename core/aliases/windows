# -*- mode: conf-space -*-

smacs      runemacs
tree       tree.com
ywd        pwd | tee /dev/tty | cygpath -f - -w | tr -d "\n" | clip

# languages
irb        winpty $(which irb).cmd

# Directories
pf@dirx           c:/Program Files
pf86@dirx         "c:/Program Files (x86)"

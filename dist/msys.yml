- defaults:
    bots:
      config: 'config.yml'
      unsafe: true

- bots:
    - ../programs/chocolatey

# packages without any configuration.
- packages:
    chocolatey:
      - wsl
      - package: cygwin
        params: /NoStartMenu
      - cyg-get
